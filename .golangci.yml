linters:
  enable:
    - govet
    - errcheck
    - staticcheck
    - gosimple
    - unused
    - ineffassign
    - typecheck
run:
  timeout: 5m
  modules-download-mode: readonly
issues:
  exclude-use-default: true
